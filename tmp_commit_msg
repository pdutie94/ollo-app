Fix: detect manual Chrome close via page count + browser disconnected

- Track non-empty page count with idle timer — when all real pages closed, trigger notifyClose
- Listen to browser.on('disconnected') for crash/kill detection
- Add debug logs for troubleshooting page lifecycle
- Fix eventBus.emit missing 'type' field causing renderer to ignore profile:stopped event
- Add console.log in App.tsx onBrowserEvent for verification

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
