Fix: auto-detect process exit — update DB & event history when Chrome closed manually

- process-manager onClose now calls updateProfile(status:stopped) + recordEvent
- Previously only emitted event but never updated DB, causing stuck "running" state

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
