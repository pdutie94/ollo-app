Phase 6: Complete Stub Implementations

- Replace fake proxy test with real window.api.proxyTest() call
- Replace fake latency with real Date.now() timing
- Display real country from ProxyTestResult instead of hardcoded "—"
- Add countryToFlag mapping for flag emoji display
- Implement real ImportDrawer with CSV/JSON parsing via IPC
- Add profile:import-file IPC handler with dialog + format detection
- Add extension:install-from-url and extension:install-from-file IPC handlers
- Update AddExtensionDrawer with real API calls and onInstalled callback
- Wire Header search bar to global search (Enter navigates to profiles)
- Implement Save Draft button (localStorage)
- Wire Header profile/logout buttons to settings/page actions
- Add globalSearch to useUIStore
- Add profileImportFile, extensionInstallFromUrl, extensionInstallFromFile to preload API
- Update type declarations in both env.d.ts and preload/index.d.ts

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
